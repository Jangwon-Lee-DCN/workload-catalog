apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: default # kpt-set: ${namespace}
  annotations:
    argocd.argoproj.io/hook: Skip
spec:
  hard:
    requests.cpu: "6"
    requests.memory: 6Gi
    limits.cpu: "6"
    limits.memory: 6Gi
